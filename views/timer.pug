extends layout
block content
    html
        head
        body
        p #{siteDate}
        div
            form(method="POST")
                button.col-sm-3.btn.btn-primary(type="submit"  role='button' href='#{link}') Start
                button.col-sm-3.btn.btn-primary(type="submit"  role='button' href='http://localhost:3000/timer') Reset
        div
        
        footer