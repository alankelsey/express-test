extends ../layout

block content
  h2 JSON
  hr
  if pageId === 'userFile'
    .row
      .col-sm-4
        a(href='/api/index', style='color: #fff')
          .panel.panel-default(style='background-color: #3b5998')
            .panel-body

